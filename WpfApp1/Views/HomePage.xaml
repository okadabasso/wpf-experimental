<Page x:Class="WpfApp1.Views.HomePage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:resources="clr-namespace:WpfApp1.Resources"
      xmlns:local="clr-namespace:WpfApp1.Views"
      xmlns:viewModels="clr-namespace:WpfApp1.ViewModels"

      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="HomePage">
    <Page.Resources>
        <FontFamily x:Key="FAFont">pack://application:,,,/WpfApp1;component/Assets/fontawesome/otfs/Font Awesome 6 Free-Solid-900.otf#Font Awesome 6 Free Solid</FontFamily>
        <Style TargetType="TabControl">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TabControl">
                        <Grid KeyboardNavigation.TabNavigation="Local">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <!-- タブヘッダー -->
                                <RowDefinition Height="*"/>
                                <!-- コンテンツ -->
                            </Grid.RowDefinitions>

                            <!-- タブヘッダー -->
                            <Grid Grid.Row="0" Panel.ZIndex="10">
                                <Border BorderBrush="{StaticResource Green300}" BorderThickness="0">
                                    <DockPanel>
                                        <StackPanel DockPanel.Dock="Right" Orientation="Horizontal" Margin="4,0,0,0" VerticalAlignment="Bottom">
                                            <Button x:Name="tabScrollLeft" Click="tabScrollLeft_Click" Width="20" Height="20" Padding="2">
                                                <TextBlock FontFamily="{StaticResource FAFont}" FontSize="12" Text="{x:Static resources:FAIcons.ChevronLeft}"/>
                                            </Button>
                                            <Button x:Name="tabScrollRight" Click="tabScrollRight_Click" Width="20" Height="20" Padding="2">
                                                <TextBlock FontFamily="{StaticResource FAFont}" FontSize="12" Text="{x:Static resources:FAIcons.ChevronRight}"/>
                                            </Button>
                                        </StackPanel>
                                        <ScrollViewer x:Name="tabScroll" Panel.ZIndex="10" HorizontalScrollBarVisibility="Hidden" VerticalScrollBarVisibility="Hidden">
                                            <ItemsPresenter />
                                        </ScrollViewer>
                                    </DockPanel>
                                </Border>
                            </Grid>

                            <!-- 赤枠（コンテンツの下） -->
                            <Border Grid.Row="1"
                            BorderBrush="{StaticResource Gray300}"
                            BorderThickness="1"
                            Panel.ZIndex="0"
                            Margin="0,-1,0,0">
                                <ContentPresenter x:Name="PART_SelectedContentHost"
                                          ContentSource="SelectedContent"/>
                            </Border>

                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="TabItem">
            <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
            <Setter Property="Width" Value="120"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TabItem">
                        <Grid x:Name="Panel" Panel.ZIndex="10">
                            <Border x:Name="Bd"
                            BorderBrush="{StaticResource Gray300}"
                            BorderThickness="1"
                            Background="{StaticResource Gray50}"
                            Padding="10,2"
                            Margin="0,0,0,0"  
                                Panel.ZIndex="0">
                                <ContentPresenter x:Name="ContentSite"
                                          ContentSource="Header"
                                          HorizontalAlignment="Center"
                                          VerticalAlignment="Center"/>
                            </Border>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <!-- アクティブタブは赤枠を完全に覆う -->
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="White"/>
                                <Setter TargetName="Bd" Property="Margin" Value="0,0,0,-1"/>
                                <Setter Property="Panel.ZIndex" Value="300"/>
                            </Trigger>
                            <!-- 非アクティブタブは赤枠が下端に見える -->
                            <Trigger Property="IsSelected" Value="False">
                                <Setter TargetName="Bd" Property="Background" Value="{StaticResource Gray100}"/>
                                <Setter TargetName="Bd" Property="Margin" Value="0,0,0,0"/>
                                <Setter Property="Panel.ZIndex" Value="10"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>


        <Style TargetType="TabPanel">
            <Setter Property="Background" Value="AliceBlue"></Setter>
        </Style>
    </Page.Resources>
    <Grid Background="White">
        <TabControl x:Name="tab" 
                    Grid.Column="0" 
                    Background="White" 
                    TabStripPlacement="Top"  
                    SelectedIndex="0">
            <TabControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <TabPanel IsItemsHost="True"/>
                </ItemsPanelTemplate>
            </TabControl.ItemsPanel>
            <TabItem Header="Home">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"></RowDefinition>
                            <RowDefinition Height="*"></RowDefinition>
                        </Grid.RowDefinitions>
                        <TextBlock Grid.Row="0" Text="Home" FontSize="{StaticResource LargeFontSize}"></TextBlock>
                    </Grid>
                </TabItem>
                <TabItem Header="About">
                    <Grid Grid.Column="1">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"></RowDefinition>
                            <RowDefinition Height="Auto"></RowDefinition>
                            <RowDefinition Height="*"></RowDefinition>
                        </Grid.RowDefinitions>
                        <Grid Grid.Row="0">
                            <TextBlock Text="About" FontSize="{StaticResource LargeFontSize}"></TextBlock>
                        </Grid>
                    </Grid>
                </TabItem>
                <TabItem Header="Contact">
                    <Grid Background="White">
                        <TextBlock Text="Contact" FontSize="{StaticResource LargeFontSize}"></TextBlock>
                    </Grid>
                </TabItem>
            </TabControl>
    </Grid>
</Page>
