<UserControl x:Class="WpfApp1.Views.TabView1"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WpfApp1.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <StackPanel Orientation="Vertical">
        <TextBlock Text="{Binding Header}" FontSize="20"/>
        <TextBlock Text="{Binding Message}" FontSize="16"/>
        <Button Content="Click Me" Command="{Binding CloseTabCommand}" Width="100" Height="30" Margin="0,10,0,0"/>
    </StackPanel>
</UserControl>
